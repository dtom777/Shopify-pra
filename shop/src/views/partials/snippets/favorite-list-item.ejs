<% if (user) { %>

<body onload="handleFavorite(<%= JSON.stringify('') %>,<%= JSON.stringify(user) %>,'load')">
  <button id="<%= product.id %>" class="fbl-favorites" type="submit"
    onclick="handleFavorite(<%= JSON.stringify(product) %>,<%= JSON.stringify(user) %>, 'click')">
    <span>♥</span>
  </button>
</body>

<% } %>
